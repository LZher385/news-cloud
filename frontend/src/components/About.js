import React from "react";

import useStyles from "../styles";

const About = () => {
  const classes = useStyles();

  return (
    <div>
      <h1>About</h1>
    </div>
  );
};

export default About;
